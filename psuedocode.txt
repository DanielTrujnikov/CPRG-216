1- read a, b, and c from the user.
2- if a == 0 then x1 = x2 = -c/b
    else
        if 4*a*c < b^2 then
        x1 = (-b +squrt(b^2 - 4*a*c))/(2*a)
        x2 = (-b -squrt(b^2 - 4*a*c))/(2*a)
        else
            print "No possible solution"
            exit
    print x1, x2

    End

1- read x, y, z
2- if x < y
        smallest = x
    else smallest = y

3- if z < smallest
        smallest = z
4- print smallest

//

 - read x, y, z
 - if x<y
    y=x
 - if z<y
    y=z
 - print y

 //

 